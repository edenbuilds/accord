# Local gateway only. Generate with: openssl rand -hex 32
ACCORD_GATEWAY_TOKEN=
ACCORD_CONFIG=gateway/example.json
PORT=4318
# Example upstream credential referenced by tokenEnv in your config:
# GITHUB_MCP_TOKEN=
